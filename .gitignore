# Duck Game mod loader build artifacts (generated next to mod.conf at runtime).
# The loader emits several name variants; match them all.
*_compiled*.dll
*_compiled*.pdb
*_compiled*.hash
*_compiled*Data.txt
*_build.log
RebuiltData.txt

# Build / IDE
obj/
bin/
.vs/
*.user
*.suo
*.sln.docstates

# OS
Thumbs.db
desktop.ini
.DS_Store
